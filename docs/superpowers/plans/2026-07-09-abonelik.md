# Abonelik & Manuel Ödeme (v1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Manuel havaleyle tahsil edilen okul aboneliklerinin dönem/ödeme kaydını tutmak ve süresi dolan/askıya alınan okulların erişimini kademeli (uyarı → kilit) kesmek.

**Architecture:** `schools.access_until` tek karar kolonu; `subscriptionState` saf fonksiyonu her istekte durumu hesaplar (cron yok). Enforcement iki katman: dashboard layout redirect (birincil) + `requireAbility` guard (ikincil). Ödeme kayıtları `school_payments` tablosuna /platform panelinden girilir; kayıt eklenince `access_until` denormalize güncellenir.

**Tech Stack:** Next.js App Router, Supabase (tipli client + service-role), Zod, Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-07-09-abonelik-design.md`

## Global Constraints

- Para **kuruş cinsinden integer** (`amount_kurus`), float YASAK.
- `access_until` **dahil** son erişim günü (o gün açık, ertesi gün kilit). Uyarı eşiği 14 gün, sabit.
- `access_until = null` → süresiz erişim (mevcut okullar etkilenmez, geriye uyumluluk).
- Enforcement **fail-open**: schools verisi okunamazsa geçir + `logger.error` (tahsilat baskısı güvenlik sınırı değil).
- Migration sonrası DB tipleri regen ZORUNLU (`database.types.ts`, yoksa tsc kırılır).
- `school_payments`: RLS enabled, policy YOK (usage_daily deseni) — advisor 0008 INFO bilinçli kabul.
- Plan gating YOK; `/veli/[token]` açık kalır; e-posta/push bildirimi YOK.
- Her task sonunda commit + push (kullanıcının kalıcı talimatı).
- Türkçe UI metinleri, Türkçe karakterler eksiksiz.

---

### Task 1: subscriptionMath saf fonksiyonu (TDD)

**Files:**
- Create: `src/domains/billing/subscriptionMath.ts`
- Test: `tests/vitest/unit/domains/billing/subscriptionMath.test.ts`

**Interfaces:**
- Consumes: —
- Produces:
  - `type SubscriptionState = 'suspended' | 'expired' | 'expiring' | 'active'`
  - `subscriptionState(s: { status: string; access_until: string | null }, today: string): SubscriptionState` — `today` = `'YYYY-MM-DD'` (İstanbul günü)
  - `kalanGun(accessUntil: string, today: string): number` — access_until − today, gün cinsinden (bugün biten = 0)
  - `UYARI_ESIGI_GUN = 14`

- [ ] **Step 1: Failing test'i yaz**

```ts
// tests/vitest/unit/domains/billing/subscriptionMath.test.ts
import { describe, it, expect } from 'vitest'
import { subscriptionState, kalanGun, UYARI_ESIGI_GUN } from '@/src/domains/billing/subscriptionMath'

const TODAY = '2026-07-09'

describe('kalanGun', () => {
  it('bugün biten için 0 döner', () => {
    expect(kalanGun('2026-07-09', TODAY)).toBe(0)
  })
  it('yarın biten için 1 döner', () => {
    expect(kalanGun('2026-07-10', TODAY)).toBe(1)
  })
  it('dün bitmiş için -1 döner', () => {
    expect(kalanGun('2026-07-08', TODAY)).toBe(-1)
  })
})

describe('subscriptionState', () => {
  it('suspended status her şeyi ezer (access_until gelecekte olsa bile)', () => {
    expect(subscriptionState({ status: 'suspended', access_until: '2027-01-01' }, TODAY)).toBe('suspended')
  })
  it('cancelled status suspended sayılır', () => {
    expect(subscriptionState({ status: 'cancelled', access_until: null }, TODAY)).toBe('suspended')
  })
  it('access_until null → süresiz erişim (geriye uyumluluk)', () => {
    expect(subscriptionState({ status: 'active', access_until: null }, TODAY)).toBe('active')
  })
  it('access_until bugün → hâlâ açık ama expiring (dahil son gün)', () => {
    expect(subscriptionState({ status: 'active', access_until: '2026-07-09' }, TODAY)).toBe('expiring')
  })
  it('access_until dün → expired', () => {
    expect(subscriptionState({ status: 'active', access_until: '2026-07-08' }, TODAY)).toBe('expired')
  })
  it('tam 14 gün kala → expiring (sınır dahil)', () => {
    expect(subscriptionState({ status: 'active', access_until: '2026-07-23' }, TODAY)).toBe('expiring')
  })
  it('15 gün kala → active', () => {
    expect(subscriptionState({ status: 'active', access_until: '2026-07-24' }, TODAY)).toBe('active')
  })
  it('trial status da aynı mekanizmadan geçer', () => {
    expect(subscriptionState({ status: 'trial', access_until: '2026-07-01' }, TODAY)).toBe('expired')
  })
  it('eşik sabiti 14', () => {
    expect(UYARI_ESIGI_GUN).toBe(14)
  })
})
```

- [ ] **Step 2: Fail'i doğrula**

Run: `npx vitest run tests/vitest/unit/domains/billing/subscriptionMath.test.ts`
Expected: FAIL — "Cannot find module '@/src/domains/billing/subscriptionMath'"

- [ ] **Step 3: Minimal implementasyonu yaz**

```ts
// src/domains/billing/subscriptionMath.ts
// Abonelik durum matematiği — saf, DB'siz. Spec: docs/superpowers/specs/2026-07-09-abonelik-design.md
// access_until DAHİL son erişim günüdür; null = süresiz (geriye uyumluluk).

export type SubscriptionState = 'suspended' | 'expired' | 'expiring' | 'active'

export const UYARI_ESIGI_GUN = 14

const GUN_MS = 86_400_000

/** access_until − today, gün cinsinden. İkisi de 'YYYY-MM-DD' (UTC parse — aynı format, fark güvenli). */
export function kalanGun(accessUntil: string, today: string): number {
  return Math.round((Date.parse(accessUntil) - Date.parse(today)) / GUN_MS)
}

export function subscriptionState(
  s: { status: string; access_until: string | null },
  today: string,
): SubscriptionState {
  if (s.status === 'suspended' || s.status === 'cancelled') return 'suspended'
  if (!s.access_until) return 'active'
  const gun = kalanGun(s.access_until, today)
  if (gun < 0) return 'expired'
  if (gun <= UYARI_ESIGI_GUN) return 'expiring'
  return 'active'
}
```

- [ ] **Step 4: Pass'i doğrula**

Run: `npx vitest run tests/vitest/unit/domains/billing/subscriptionMath.test.ts`
Expected: 12 passed

- [ ] **Step 5: Commit**

```bash
git add src/domains/billing/subscriptionMath.ts tests/vitest/unit/domains/billing/subscriptionMath.test.ts
git commit -m "feat: subscriptionMath — abonelik durum hesabı (TDD)"
git push origin main
```

---

### Task 2: Migration — access_until + school_payments + DB tipleri regen

> **NOT:** Bu task Supabase MCP araçları gerektirir (`apply_migration`, `generate_typescript_types`) — subagent'ta MCP yoksa ana oturum yürütür.

**Files:**
- Create: `supabase/migrations/20260709120000_billing.sql`
- Modify: `src/infrastructure/supabase/database.types.ts` (regen ile)

**Interfaces:**
- Consumes: —
- Produces: `schools.access_until date NULL` kolonu; `school_payments` tablosu (kolonlar aşağıda). Sonraki task'lar tipli client üzerinden bu şemayı kullanır.

- [ ] **Step 1: Migration dosyasını yaz**

```sql
-- supabase/migrations/20260709120000_billing.sql
-- Abonelik v1: tek karar kolonu + manuel ödeme kayıtları.
-- Spec: docs/superpowers/specs/2026-07-09-abonelik-design.md

-- access_until: DAHİL son erişim günü. NULL = süresiz (mevcut okullar etkilenmez).
alter table public.schools add column if not exists access_until date;

create table if not exists public.school_payments (
  id            uuid primary key default gen_random_uuid(),
  school_id     uuid not null references public.schools(id),
  amount_kurus  integer not null check (amount_kurus > 0),
  paid_at       date not null,
  period_start  date not null,
  period_end    date not null,
  note          text,
  created_by    uuid not null,
  created_at    timestamptz not null default now(),
  constraint school_payments_period check (period_end >= period_start)
);

create index if not exists school_payments_school_idx on public.school_payments (school_id, period_end desc);

-- Policy YOK (usage_daily deseni): tüm erişim /platform service-client action'ından,
-- platform_admins guard'lı. Advisor 0008 INFO bilinçli kabul.
alter table public.school_payments enable row level security;

-- PostgREST üzerinden anon/authenticated dokunamasın (RLS zaten kilitli ama default grant'leri da sık).
revoke all on public.school_payments from anon, authenticated;

-- tenant_metrics view'ına access_until ekle (mevcut tanım: 20260615123000_drop_export_jobs.sql).
drop view if exists public.tenant_metrics;
create view public.tenant_metrics as
 SELECT s.id          AS school_id,
        s.name        AS school_name,
        s.slug,
        s.status,
        s.access_until,
        s.created_at,
        count(DISTINCT CASE
            WHEN p.role = ANY (ARRAY['ogretmen'::text, 'zumre_baskani'::text, 'mudur_yardimcisi'::text]) THEN p.id
            ELSE NULL::uuid
        END)          AS teacher_count,
        count(DISTINCT CASE
            WHEN p.role = 'mudur'::text THEN p.id
            ELSE NULL::uuid
        END)          AS mudur_count,
        count(DISTINCT st.id) AS student_count,
        count(DISTINCT c.id)  AS class_count,
        count(DISTINCT h.id)  AS homework_count
   FROM schools s
     LEFT JOIN profiles  p  ON p.school_id  = s.id
     LEFT JOIN students  st ON st.school_id = s.id AND st.deleted_at IS NULL
     LEFT JOIN classes   c  ON c.school_id  = s.id AND c.deleted_at IS NULL
     LEFT JOIN homeworks h  ON h.school_id  = s.id AND h.deleted_at IS NULL
  GROUP BY s.id, s.name, s.slug, s.status, s.access_until, s.created_at;
```

- [ ] **Step 2: Canlıya uygula**

Supabase MCP: `apply_migration(project_id: 'agijvfrcudpzsofgfogu', name: 'billing', query: <yukarıdaki SQL>)`
Expected: success

- [ ] **Step 3: DB tiplerini regen et**

Supabase MCP: `generate_typescript_types(project_id: 'agijvfrcudpzsofgfogu')` → çıktıyı `src/infrastructure/supabase/database.types.ts` dosyasına yaz.
Expected: dosyada `school_payments` ve `schools.access_until` görünür.

- [ ] **Step 4: tsc + unit doğrula**

Run: `npx tsc --noEmit && npm run test:unit`
Expected: tsc temiz, 805 passed (793 + Task 1'in 12'si)

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260709120000_billing.sql src/infrastructure/supabase/database.types.ts
git commit -m "feat: billing migration — schools.access_until + school_payments"
git push origin main
```

---

### Task 3: getCurrentProfile join genişletmesi (status + access_until)

**Files:**
- Modify: `src/shared/auth/index.ts:12-27` (getCurrentProfile)

**Interfaces:**
- Consumes: Task 2'nin `schools.access_until` kolonu.
- Produces: `getCurrentProfile()` dönüşünde `schools: { name: string; slug: string; status: string; access_until: string | null } | null`. Task 4/5/6 bu alanları okur.

- [ ] **Step 1: Select ve dönüş tipini genişlet**

`src/shared/auth/index.ts` içinde getCurrentProfile'da:

```ts
    .select('id, full_name, subject, role, school_id, avatar_url, created_at, schools(name, slug, status, access_until)')
```

ve dönüş cast'inde:

```ts
    schools: { name: string; slug: string; status: string; access_until: string | null } | null
```

(Mevcut cast bloğunun yalnız `schools` satırı değişir; alan EKLEMEK structural typing'de mevcut tüketicileri kırmaz.)

- [ ] **Step 2: tsc + unit doğrula**

Run: `npx tsc --noEmit && npm run test:unit`
Expected: temiz. (Kırılırsa: profile.schools'u dar tiple bekleyen tüketici vardır — dar tipi genişlet, cast EKLEME.)

- [ ] **Step 3: Commit**

```bash
git add src/shared/auth/index.ts
git commit -m "feat: getCurrentProfile schools join'ine status + access_until"
git push origin main
```

---

### Task 4: Kilit sayfası /abonelik-gerekli + layout enforcement

**Files:**
- Create: `app/abonelik-gerekli/page.tsx`
- Modify: `app/(dashboard)/layout.tsx` (profile alındıktan sonra redirect)

**Interfaces:**
- Consumes: Task 1 `subscriptionState`, Task 3 `profile.schools.status/access_until`.
- Produces: `expired|suspended` okul kullanıcısı dashboard'a giremez; `/abonelik-gerekli` sayfası.

- [ ] **Step 1: Kilit sayfasını yaz**

```tsx
// app/abonelik-gerekli/page.tsx
import { redirect } from 'next/navigation'
import { getCurrentProfile } from '@/src/shared/auth'
import { isMudurOrAbove } from '@/src/shared/types'
import { subscriptionState } from '@/src/domains/billing/subscriptionMath'
import { todayLocalISO } from '@/src/shared/date'
import { logout } from '@/app/actions/auth'

export const dynamic = 'force-dynamic'
export const metadata = { title: 'Abonelik Gerekli', robots: { index: false, follow: false } }

export default async function AbonelikGerekliPage() {
  const profile = await getCurrentProfile()
  if (!profile?.school_id) redirect('/login')

  // Abonelik aktifse burada işi yok — kilitli olmayan kullanıcı takılı kalmasın.
  const state = profile.schools
    ? subscriptionState(profile.schools, todayLocalISO())
    : 'active'
  if (state !== 'expired' && state !== 'suspended') redirect('/anasayfa')

  const yonetici = isMudurOrAbove(profile.role)

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 dark:bg-slate-950 px-4">
      <div className="max-w-md w-full bg-white dark:bg-slate-800 border border-gray-200 dark:border-slate-700 rounded-2xl p-8 text-center">
        <p className="text-5xl">🔒</p>
        <h1 className="text-xl font-semibold text-gray-900 dark:text-slate-100 mt-4">
          {profile.schools?.name ?? 'Okulunuz'} — abonelik sona erdi
        </h1>
        <p className="text-sm text-gray-500 dark:text-slate-400 mt-3">
          {yonetici
            ? 'Aboneliğinizi yenilemek için bizimle iletişime geçin. Ödemeniz işlendiğinde erişim kendiliğinden açılır; verileriniz güvende.'
            : 'Okulunuzun aboneliği sona erdi. Lütfen okul yöneticinizle görüşün; verileriniz güvende.'}
        </p>
        <form action={logout} className="mt-6">
          <button
            type="submit"
            className="border border-gray-300 dark:border-slate-600 text-gray-700 dark:text-slate-300 px-5 py-2.5 rounded-lg text-sm font-medium hover:bg-gray-50 dark:hover:bg-slate-700 transition-colors"
          >
            Çıkış yap
          </button>
        </form>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Layout'a enforcement ekle**

`app/(dashboard)/layout.tsx` — import'lara ekle:

```ts
import { subscriptionState } from '@/src/domains/billing/subscriptionMath'
import { todayLocalISO } from '@/src/shared/date'
import { logger } from '@/src/infrastructure/observability/logger'
```

`const profile = await getCurrentProfile()` satırından SONRA, onboarding kontrolünden ÖNCE:

```ts
  // Abonelik enforcement (fail-open: schools okunamazsa geçir — tahsilat güvenlik sınırı değil).
  if (profile?.school_id) {
    if (!profile.schools) {
      logger.error({ event: 'abonelik_schools_missing', userId: profile.id }, 'Profile schools join boş — enforcement atlandı')
    } else {
      const state = subscriptionState(profile.schools, todayLocalISO())
      if (state === 'expired' || state === 'suspended') redirect('/abonelik-gerekli')
    }
  }
```

- [ ] **Step 3: tsc doğrula + manuel smoke**

Run: `npx tsc --noEmit`
Expected: temiz.
Smoke: `npm run dev` → normal kullanıcıyla `/anasayfa` açılıyor (access_until null → değişiklik yok), `/abonelik-gerekli` elle açılınca `/anasayfa`'ya geri yönleniyor.

- [ ] **Step 4: Commit**

```bash
git add app/abonelik-gerekli/page.tsx "app/(dashboard)/layout.tsx"
git commit -m "feat: abonelik kilit sayfası + dashboard layout enforcement"
git push origin main
```

---

### Task 5: requireAbility yazma guard'ı

**Files:**
- Modify: `src/shared/authorization/index.ts` (DenialReason union + buildMessage)
- Modify: `src/shared/authorization/server.ts:29-39` (requireAbility)

**Interfaces:**
- Consumes: Task 1 `subscriptionState`, Task 3 profile join'i.
- Produces: `requireAbility()` expired/suspended okulda `AuthorizationError({ code: 'SUBSCRIPTION_EXPIRED', permission: '*' })` fırlatır. Mevcut tüm servisler otomatik korunur.

- [ ] **Step 1: DenialReason'a kod ekle**

`src/shared/authorization/index.ts` içinde `DenialReason` union'ının code alanına `'SUBSCRIPTION_EXPIRED'` ekle (mevcut union'ı bul, tek değer ekle) ve `buildMessage`'a:

```ts
  if (d.code === 'SUBSCRIPTION_EXPIRED') return 'Okul aboneliği sona erdi'
```

- [ ] **Step 2: requireAbility'ye guard ekle**

`src/shared/authorization/server.ts` — import'lara:

```ts
import { getCurrentProfile } from '@/src/shared/auth'
import { subscriptionState } from '@/src/domains/billing/subscriptionMath'
import { todayLocalISO } from '@/src/shared/date'
```

(`getCurrentProfile` zaten import ediliyorsa yinelenme — mevcut import satırını kullan.)

`requireAbility` içinde, `return ability` satırından ÖNCE:

```ts
  // Abonelik guard'ı: layout redirect'ini soft-nav ile atlayan oturum da yazamasın.
  // getCurrentProfile cache()'li — aynı istekte ikinci çağrı bedava.
  const profile = await getCurrentProfile()
  if (profile?.schools) {
    const state = subscriptionState(profile.schools, todayLocalISO())
    if (state === 'expired' || state === 'suspended') {
      securityLog('auth.subscription_expired_action', { userId: ability.userId })
      throw new AuthorizationError({ code: 'SUBSCRIPTION_EXPIRED', permission: '*' })
    }
  }
```

- [ ] **Step 3: tsc + unit doğrula**

Run: `npx tsc --noEmit && npm run test:unit`
Expected: temiz, 805 passed. (securityLog imzası farklıysa dosyadaki mevcut kullanım deseni birebir kopyalanır.)

- [ ] **Step 4: Commit**

```bash
git add src/shared/authorization/index.ts src/shared/authorization/server.ts
git commit -m "feat: requireAbility abonelik guard'ı (SUBSCRIPTION_EXPIRED)"
git push origin main
```

---

### Task 6: Uyarı bandı (müdür/MY, expiring)

**Files:**
- Create: `app/(dashboard)/AbonelikUyariSeridi.tsx`
- Modify: `app/(dashboard)/layout.tsx` (koşullu render)

**Interfaces:**
- Consumes: Task 1 `kalanGun`/`subscriptionState`, Task 3 profile join'i.
- Produces: expiring durumda müdür/MY'ye amber bant. Snooze YOK (bilinçli).

- [ ] **Step 1: Bandı yaz (saf server component)**

```tsx
// app/(dashboard)/AbonelikUyariSeridi.tsx
// Abonelik bitiş uyarısı — yalnız müdür/MY görür, snooze yok (tahsilat baskısı bilinçli).
export default function AbonelikUyariSeridi({ kalan }: { kalan: number }) {
  return (
    <div className="bg-amber-50 dark:bg-amber-950/40 border-b border-amber-200 dark:border-amber-900 px-4 py-2.5 text-center">
      <p className="text-sm text-amber-800 dark:text-amber-300">
        Okulunuzun aboneliği{' '}
        <strong>{kalan === 0 ? 'bugün' : `${kalan} gün sonra`}</strong> sona erecek.
        Kesinti yaşamamak için lütfen bizimle iletişime geçin.
      </p>
    </div>
  )
}
```

- [ ] **Step 2: Layout'ta koşullu render**

`app/(dashboard)/layout.tsx` — import:

```ts
import AbonelikUyariSeridi from './AbonelikUyariSeridi'
import { subscriptionState, kalanGun } from '@/src/domains/billing/subscriptionMath'
import { isMudurOrAbove } from '@/src/shared/types'
```

(`subscriptionState` Task 4'te import edildi; `isTeachingRole` zaten import'ta — aynı satıra `isMudurOrAbove` eklenir.)

Task 4'teki enforcement bloğunun hesapladığı state'i değişkende tut ve `<main>` içinde `<PushTesvikSeridi />`'nin hemen önüne:

```tsx
{abonelikUyari !== null && <AbonelikUyariSeridi kalan={abonelikUyari} />}
```

Layout gövdesinde (enforcement bloğunu şu şekle genişlet):

```ts
  let abonelikUyari: number | null = null
  if (profile?.school_id) {
    if (!profile.schools) {
      logger.error({ event: 'abonelik_schools_missing', userId: profile.id }, 'Profile schools join boş — enforcement atlandı')
    } else {
      const today = todayLocalISO()
      const state = subscriptionState(profile.schools, today)
      if (state === 'expired' || state === 'suspended') redirect('/abonelik-gerekli')
      if (state === 'expiring' && isMudurOrAbove(profile.role) && profile.schools.access_until) {
        abonelikUyari = kalanGun(profile.schools.access_until, today)
      }
    }
  }
```

- [ ] **Step 3: tsc doğrula**

Run: `npx tsc --noEmit`
Expected: temiz.

- [ ] **Step 4: Commit**

```bash
git add "app/(dashboard)/AbonelikUyariSeridi.tsx" "app/(dashboard)/layout.tsx"
git commit -m "feat: abonelik bitiş uyarı bandı (müdür/MY, 14 gün)"
git push origin main
```

---

### Task 7: Ödeme kaydı — platform action'ları + Ödemeler modalı

**Files:**
- Modify: `app/platform/actions.ts` (recordPayment + listSchoolPayments)
- Create: `app/platform/PaymentsModal.tsx`
- Modify: `app/platform/page.tsx` (okul satırına buton + access_until gösterimi)

**Interfaces:**
- Consumes: Task 2 `school_payments` tablosu + tipleri.
- Produces:
  - `recordPayment(prev, formData): Promise<{ error?: string; ok?: boolean }>` — formData alanları: `school_id, amount_tl, paid_at, period_start, period_end, note`
  - `listSchoolPayments(schoolId: string): Promise<{ error?: string; payments?: PaymentRow[] }>` — `PaymentRow = { id: string; amount_kurus: number; paid_at: string; period_start: string; period_end: string; note: string | null }`

- [ ] **Step 1: Action'ları yaz**

`app/platform/actions.ts` sonuna ekle:

```ts
const PaymentSchema = z.object({
  school_id:    z.string().uuid(),
  amount_tl:    z.coerce.number().positive().max(1_000_000),
  paid_at:      z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  period_start: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  period_end:   z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  note:         z.string().max(300).optional().or(z.literal('')),
}).refine(p => p.period_end >= p.period_start, { message: 'Dönem sonu başlangıçtan önce olamaz' })

export type PaymentRow = {
  id: string
  amount_kurus: number
  paid_at: string
  period_start: string
  period_end: string
  note: string | null
}

export async function recordPayment(
  _prev: { error?: string; ok?: boolean } | null,
  formData: FormData,
): Promise<{ error?: string; ok?: boolean }> {
  const supabase = await requirePlatformAdmin()
  if (!supabase) return { error: 'Yetkisiz' }

  const parsed = PaymentSchema.safeParse({
    school_id:    formData.get('school_id'),
    amount_tl:    formData.get('amount_tl'),
    paid_at:      formData.get('paid_at'),
    period_start: formData.get('period_start'),
    period_end:   formData.get('period_end'),
    note:         formData.get('note'),
  })
  if (!parsed.success) return { error: parsed.error.issues[0]?.message ?? 'Geçersiz form' }
  const p = parsed.data

  const user = await getCurrentUser()
  const { error: insErr } = await supabase.from('school_payments').insert({
    school_id:    p.school_id,
    amount_kurus: Math.round(p.amount_tl * 100),
    paid_at:      p.paid_at,
    period_start: p.period_start,
    period_end:   p.period_end,
    note:         p.note || null,
    created_by:   user!.id,
  })
  if (insErr) return { error: `Kayıt başarısız: ${insErr.message}` }

  // access_until = max(mevcut, period_end). Ayrı adım; başarısızlık admin'e görünür (v1 kabul).
  const { data: school } = await supabase.from('schools').select('access_until').eq('id', p.school_id).single()
  if (!school || (school.access_until ?? '') < p.period_end) {
    const { error: updErr } = await supabase.from('schools').update({ access_until: p.period_end }).eq('id', p.school_id)
    if (updErr) return { error: `Ödeme kaydedildi ama erişim tarihi güncellenemedi: ${updErr.message}` }
  }

  revalidatePath('/platform')
  return { ok: true }
}

export async function listSchoolPayments(schoolId: string): Promise<{ error?: string; payments?: PaymentRow[] }> {
  const supabase = await requirePlatformAdmin()
  if (!supabase) return { error: 'Yetkisiz' }
  if (typeof schoolId !== 'string') return { error: 'Geçersiz istek' }

  const { data, error } = await supabase
    .from('school_payments')
    .select('id, amount_kurus, paid_at, period_start, period_end, note')
    .eq('school_id', schoolId)
    .order('period_end', { ascending: false })
    .limit(50)
  if (error) return { error: error.message }
  return { payments: data }
}
```

DİKKAT: `school.access_until ?? ''` karşılaştırması — null (süresiz) okulda `'' < period_end` true olur ve süresiz okul TARİHLİ hale gelir. Bu bilinçli: ödeme kaydı girilen okul artık dönemli takip edilir. (Süresiz kalması istenen okula ödeme kaydı girilmez.)

- [ ] **Step 2: Modalı yaz**

```tsx
// app/platform/PaymentsModal.tsx
'use client'

import { useActionState, useEffect, useState } from 'react'
import { recordPayment, listSchoolPayments, type PaymentRow } from './actions'

const tl = (kurus: number) =>
  (kurus / 100).toLocaleString('tr-TR', { style: 'currency', currency: 'TRY' })

export default function PaymentsModal({ schoolId, schoolName }: { schoolId: string; schoolName: string }) {
  const [open, setOpen] = useState(false)
  const [payments, setPayments] = useState<PaymentRow[] | null>(null)
  const [state, formAction, pending] = useActionState(recordPayment, null)

  useEffect(() => {
    if (!open) return
    listSchoolPayments(schoolId).then(r => setPayments(r.payments ?? []))
  }, [open, schoolId, state?.ok])

  if (!open) {
    return (
      <button
        onClick={() => setOpen(true)}
        className="text-xs text-slate-400 border border-slate-700 rounded-lg px-3 py-1 hover:text-slate-200 hover:border-slate-500 transition-colors"
      >
        Ödemeler
      </button>
    )
  }

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4" onClick={() => setOpen(false)}>
      <div className="bg-slate-900 border border-slate-700 rounded-2xl p-6 w-full max-w-lg max-h-[85vh] overflow-y-auto" onClick={e => e.stopPropagation()}>
        <div className="flex items-center justify-between mb-4">
          <h2 className="text-white font-semibold">{schoolName} — Ödemeler</h2>
          <button onClick={() => setOpen(false)} className="text-slate-500 hover:text-slate-300">✕</button>
        </div>

        {payments === null ? (
          <p className="text-slate-500 text-sm">Yükleniyor…</p>
        ) : payments.length === 0 ? (
          <p className="text-slate-500 text-sm">Henüz ödeme kaydı yok.</p>
        ) : (
          <table className="w-full text-sm mb-5">
            <thead>
              <tr className="text-slate-500 text-xs text-left">
                <th className="pb-2">Ödeme</th><th className="pb-2">Tutar</th><th className="pb-2">Dönem</th>
              </tr>
            </thead>
            <tbody>
              {payments.map(p => (
                <tr key={p.id} className="border-t border-slate-800 text-slate-300">
                  <td className="py-2">{p.paid_at}</td>
                  <td className="py-2">{tl(p.amount_kurus)}</td>
                  <td className="py-2 text-slate-400">{p.period_start} → {p.period_end}</td>
                </tr>
              ))}
            </tbody>
          </table>
        )}

        <form action={formAction} className="space-y-3 border-t border-slate-800 pt-4">
          <input type="hidden" name="school_id" value={schoolId} />
          <div className="grid grid-cols-2 gap-3">
            <label className="text-xs text-slate-400">Tutar (TL)
              <input name="amount_tl" type="number" step="0.01" min="1" required
                className="mt-1 w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-slate-200 text-sm" />
            </label>
            <label className="text-xs text-slate-400">Ödeme tarihi
              <input name="paid_at" type="date" required
                className="mt-1 w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-slate-200 text-sm" />
            </label>
            <label className="text-xs text-slate-400">Dönem başı
              <input name="period_start" type="date" required
                className="mt-1 w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-slate-200 text-sm" />
            </label>
            <label className="text-xs text-slate-400">Dönem sonu
              <input name="period_end" type="date" required
                className="mt-1 w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-slate-200 text-sm" />
            </label>
          </div>
          <label className="text-xs text-slate-400 block">Not
            <input name="note" maxLength={300} placeholder="Havale açıklaması vb."
              className="mt-1 w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-slate-200 text-sm" />
          </label>
          {state?.error && <p className="text-red-400 text-xs">{state.error}</p>}
          {state?.ok && <p className="text-emerald-400 text-xs">Ödeme kaydedildi, erişim tarihi güncellendi.</p>}
          <button type="submit" disabled={pending}
            className="w-full bg-violet-600 hover:bg-violet-500 disabled:opacity-50 text-white rounded-lg py-2 text-sm font-medium transition-colors">
            {pending ? 'Kaydediliyor…' : 'Ödeme Kaydet'}
          </button>
        </form>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: page.tsx'e bağla**

`app/platform/page.tsx`: `import PaymentsModal from './PaymentsModal'` ekle; okul satırında (page.tsx:92-93) `StatusToggle`/`CancelSchoolButton`'ın yanına:

```tsx
<PaymentsModal schoolId={school.school_id!} schoolName={school.school_name ?? ''} />
```

ve satırda erişim bitişini göster (aynı satır grubuna, mevcut hücre düzenine uyarak):

```tsx
<span className="text-xs text-slate-500">{school.access_until ? `Erişim: ${school.access_until}` : 'Süresiz'}</span>
```

(`tenant_metrics` view'ına `access_until` Task 2 migration'ında eklendi; alan adları view'da `school_id`/`school_name` olarak alias'lı — `s.id` DEĞİL.)

- [ ] **Step 4: tsc + smoke**

Run: `npx tsc --noEmit`
Expected: temiz.
Smoke: dev'de /platform → Ödemeler modalı açılır, kayıt girilir, DB'de satır + `schools.access_until` güncellenir (SQL ile doğrula), listede görünür.

- [ ] **Step 5: Commit**

```bash
git add app/platform/
git commit -m "feat: platform ödeme kaydı — school_payments action'ları + Ödemeler modalı"
git push origin main
```

---

### Task 8: Trial bitişi — NewSchoolModal + StatusToggle

**Files:**
- Modify: `app/platform/actions.ts` (NewSchoolSchema + createSchool + updateSchoolStatus)
- Modify: `app/platform/NewSchoolModal.tsx` (trial bitiş tarihi alanı)

**Interfaces:**
- Consumes: Task 2 `access_until` kolonu.
- Produces: trial okul oluşturma/geçişte `trial_ends_at` + `access_until` birlikte yazılır (boşluk kapanır: trial'ın artık gerçek bitişi var).

- [ ] **Step 1: createSchool'u genişlet**

`NewSchoolSchema`'ya alan ekle ve refine et:

```ts
  trial_ends:   z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional().or(z.literal('')),
```

Schema'nın sonuna (object'ten sonra):

```ts
.refine(s => s.status !== 'trial' || !!s.trial_ends, { message: 'Trial için bitiş tarihi zorunlu' })
```

`createSchool` içinde parse objesine `trial_ends: formData.get('trial_ends')` ekle; insert'i şu hale getir:

```ts
  const trialEnd = status === 'trial' && parsed.data.trial_ends ? parsed.data.trial_ends : null
  const { data: school, error } = await supabase
    .from('schools')
    .insert({ name, slug, status, trial_ends_at: trialEnd ? `${trialEnd}T23:59:59+03:00` : null, access_until: trialEnd })
    // ...mevcut .select() zinciri aynı kalır
```

- [ ] **Step 2: updateSchoolStatus'u genişlet**

```ts
export async function updateSchoolStatus(schoolId: string, status: 'active' | 'trial' | 'suspended') {
  const supabase = await requirePlatformAdmin()
  if (!supabase) return
  const patch: { status: string; trial_ends_at?: string; access_until?: string } = { status }
  if (status === 'trial') {
    // Panelde tarih sorusu yok — default 30 gün; incelik Ödemeler/SQL ile. ponytail: yeterli.
    const end = new Date(Date.now() + 30 * 86_400_000).toISOString().slice(0, 10)
    patch.trial_ends_at = `${end}T23:59:59+03:00`
    patch.access_until = end
  }
  await supabase.from('schools').update(patch).eq('id', schoolId)
  revalidatePath('/platform')
}
```

(Mevcut fonksiyonun gövdesi bu şekle getirilir; imza aynı kalır — StatusToggle değişmez.)

- [ ] **Step 3: NewSchoolModal'a tarih alanı**

Modal client component; `status` select'inin mevcut `defaultValue="trial"` durumunu state'e bağla:

```tsx
const [status, setStatus] = useState('trial')
```

Select'e `value={status} onChange={e => setStatus(e.target.value)}` ekle ve select'in altına:

```tsx
{status === 'trial' && (
  <label className="text-xs text-slate-400 block">Trial bitişi
    <input name="trial_ends" type="date" required
      className="mt-1 w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-slate-200 text-sm" />
  </label>
)}
```

(Sınıf adları modaldaki mevcut input'larla birebir aynı yapılır — dosyadaki deseni kopyala.)

- [ ] **Step 4: tsc + smoke**

Run: `npx tsc --noEmit`
Expected: temiz.
Smoke: /platform → yeni okul (trial + tarih) → DB'de `trial_ends_at` ve `access_until` dolu.

- [ ] **Step 5: Commit**

```bash
git add app/platform/actions.ts app/platform/NewSchoolModal.tsx
git commit -m "feat: trial bitişi artık gerçek — access_until birlikte yazılır"
git push origin main
```

---

### Task 9: E2E testler + final doğrulama

**Files:**
- Create: `tests/playwright/e2e/abonelik.spec.ts`

**Interfaces:**
- Consumes: tüm önceki task'lar; `tests/playwright/fixtures/auth.fixture.ts` storage state'leri; `.env.local` service-role key.

**Güvenlik notu:** playwright.config `workers: 1` + `fullyParallel: false` → testler sıralı; spec kendi içinde `access_until` set/reset yapar, diğer spec'lerle çakışmaz. Reset hem beforeAll (kendini onarma) hem afterAll'da yapılır — yarıda kesilen önceki koşu kalıntısı temizlenir.

- [ ] **Step 1: E2E spec'i yaz**

```ts
// tests/playwright/e2e/abonelik.spec.ts
import { test, expect } from '@playwright/test'
import { createClient } from '@supabase/supabase-js'
import { AUTH_PATHS } from '../setup/global-setup'

const db = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!)

async function testSchoolId(): Promise<string> {
  const { data } = await db.from('profiles').select('school_id').ilike('full_name', '%test%').not('school_id', 'is', null).limit(1).single()
  if (!data?.school_id) throw new Error('Test okulu bulunamadı')
  return data.school_id
}

function isoShift(days: number): string {
  return new Date(Date.now() + days * 86_400_000).toISOString().slice(0, 10)
}

test.describe('Abonelik enforcement', () => {
  let schoolId: string

  test.beforeAll(async () => {
    schoolId = await testSchoolId()
    await db.from('schools').update({ access_until: null }).eq('id', schoolId) // kendini onarma
  })

  test.afterAll(async () => {
    await db.from('schools').update({ access_until: null }).eq('id', schoolId)
  })

  test('expiring: müdür bandı görür, öğretmen görmez', async ({ browser }) => {
    await db.from('schools').update({ access_until: isoShift(7) }).eq('id', schoolId)

    const mudurCtx = await browser.newContext({ storageState: AUTH_PATHS.mudur })
    const mudur = await mudurCtx.newPage()
    await mudur.goto('/anasayfa')
    await expect(mudur.getByText(/aboneliği .* sona erecek/i)).toBeVisible()
    await mudurCtx.close()

    const ogrCtx = await browser.newContext({ storageState: AUTH_PATHS.ogretmen })
    const ogr = await ogrCtx.newPage()
    await ogr.goto('/anasayfa')
    await expect(ogr.getByText(/sona erecek/i)).toHaveCount(0)
    await ogrCtx.close()
  })

  test('expired: kullanıcı kilit sayfasına yönlenir', async ({ browser }) => {
    await db.from('schools').update({ access_until: isoShift(-1) }).eq('id', schoolId)

    const ctx = await browser.newContext({ storageState: AUTH_PATHS.ogretmen })
    const page = await ctx.newPage()
    await page.goto('/anasayfa')
    await expect(page).toHaveURL(/abonelik-gerekli/)
    await expect(page.getByText(/abonelik sona erdi/i)).toBeVisible()
    await ctx.close()
  })

  test('null access_until: hiçbir değişiklik yok (geriye uyumluluk)', async ({ browser }) => {
    await db.from('schools').update({ access_until: null }).eq('id', schoolId)

    const ctx = await browser.newContext({ storageState: AUTH_PATHS.ogretmen })
    const page = await ctx.newPage()
    await page.goto('/anasayfa')
    await expect(page).toHaveURL(/anasayfa/)
    await expect(page.getByText(/sona erecek/i)).toHaveCount(0)
    await ctx.close()
  })
})
```

NOT: `testSchoolId` sorgusu test kullanıcılarının okulunu bulamazsa (`ilike '%test%'` isim deseni tutmazsa) global-setup'taki TEST_USERS e-postalarından `auth.users` üzerinden gidilir — spec'i çalıştırıp ilk hatada sorgu düzeltilir; test okulu tektir.

- [ ] **Step 2: Yalnız bu spec'i koştur**

Run: `npx playwright test tests/playwright/e2e/abonelik.spec.ts`
Expected: 3 passed

- [ ] **Step 3: Tam final doğrulama**

Run: `npx tsc --noEmit && npm run test:unit && npm run build && npm run test:e2e`
Expected: tsc temiz; 805 unit; build exit 0; 75/75 e2e (72 + 3).

- [ ] **Step 4: Commit + memory güncelle**

```bash
git add tests/playwright/e2e/abonelik.spec.ts
git commit -m "test: abonelik enforcement e2e (bant + kilit + geriye uyumluluk)"
git push origin main
```

Memory: proje dosyasına "Abonelik v1 TAMAMLANDI" bölümü + MEMORY.md index satırı güncellenir.

---

## Self-Review Sonucu

- **Spec kapsaması:** karar modeli→T1, veri modeli→T2, profile join→T3, kilit+layout→T4, requireAbility→T5, bant→T6, ödeme paneli→T7, trial boşluğu→T8, test→T9. Kilit sayfası içerik kuralları (müdür/öğretmen ayrımı, çıkış, IBAN yok) T4'te; fail-open T4/T5'te; kapsam-dışılar Global Constraints'te. Boşluk yok.
- **Placeholder taraması:** tenant_metrics view recreate'i keşif sonrası kesinleştirildi (T2 migration'ına gömüldü, alias'lar `school_id/school_name`). Tek koşullu nokta testSchoolId sorgusu (T9) — "çalıştır, hataya göre düzelt" talimatıyla kapatıldı, kör nokta değil.
- **Tip tutarlılığı:** `subscriptionState/kalanGun/UYARI_ESIGI_GUN` (T1) ↔ T4/T5/T6 kullanımları; `PaymentRow` (T7) modal ile aynı; `AUTH_PATHS` (T9) global-setup'tan import — tutarlı.
